<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<!-- Casting finds televisions by SSDP, which is a multicast on the local
	     network. Without this string macOS 15 refuses the socket outright and
	     never prompts, so discovery silently finds nothing. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>Vidra 需要访问本地网络来查找可投屏的电视。</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- Disable the experimental merged UI+platform thread (default-on in
	     Flutter 3.44 macOS). In this multi-window app it left a background
	     window's engine dormant — clicks stopped registering, state froze,
	     and interactions were clobbered when a lifecycle event finally kicked
	     a frame. The traditional separate-thread model keeps every window's
	     engine active. -->
	<key>FLTEnableMergedPlatformUIThread</key>
	<false/>
</dict>
</plist>
